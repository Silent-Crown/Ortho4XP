---
phase: quick
plan: 260826-f6k
type: execute
wave: 1
depends_on: []
files_modified:
  - src/O4_CLI_Utils.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "`build --high-zl` sets tile.cover_airports_with_highres = \"ICAO\" before the pipeline runs"
    - "`build --cover-zl N` sets tile.cover_zl = N before the pipeline runs"
    - "Both flags thread through single-tile build and ICAO/batch build the same way --provider/--zl already do"
  artifacts:
    - src/O4_CLI_Utils.py
  key_links:
    - "dispatch -> run_batch_build -> run_build -> tile.cover_airports_with_highres / tile.cover_zl"
---

<objective>
Add `--high-zl` and `--cover-zl` flags to the CLI `build` subcommand, mirroring the
existing `--provider`/`--zl` wiring exactly (D: high-zl sets
`tile.cover_airports_with_highres = "ICAO"`, cover-zl sets `tile.cover_zl`).

Purpose: let CLI builds request higher-detail airport textures without editing a
per-tile config file by hand — the DSF stage already reads both tile attributes.
Output: two new argparse flags, two new params threaded through
`run_batch_build`/`run_build`/`dispatch`, and self-check assertions covering them.
</objective>

<context>
@src/O4_CLI_Utils.py
@src/O4_Cfg_Vars.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire --high-zl / --cover-zl through build_parser, run_build, run_batch_build, dispatch</name>
  <files>src/O4_CLI_Utils.py</files>
  <action>
Mirror the existing --provider/--zl pattern exactly, do not invent new abstractions.

In `build_parser()`, on the `build` subparser right after the existing `--zl` argument
add: `build_p.add_argument("--high-zl", dest="high_zl", action="store_true", help="Upgrade airport textures to higher zoom level (sets cover_airports_with_highres=ICAO)")`
and `build_p.add_argument("--cover-zl", dest="cover_zl", type=int, default=None, help="Zoom level to cover airports with when --high-zl is set")`.

In `run_build(lat, lon, provider=None, zl=None, build_dir="", high_zl=False, cover_zl=None)`
append the two new params at the end of the signature (keep existing param order/positions
unchanged so nothing downstream that calls with the old positional shape breaks). After the
existing `if zl is not None: tile.default_zl = zl` block, add: `if high_zl: tile.cover_airports_with_highres = "ICAO"`
then `if cover_zl is not None: tile.cover_zl = cover_zl`.

In `run_batch_build(idents, radius, provider=None, zl=None, build_dir="", high_zl=False, cover_zl=None)`
append the same two params at the end of the signature, and pass them through to the
`run_build(lat, lon, provider, zl, build_dir)` call inside the tile loop, updating it to
`run_build(lat, lon, provider, zl, build_dir, high_zl, cover_zl)`.

In `dispatch()`, both call sites under `args.command == "build"` append `args.high_zl,
args.cover_zl` positionally after `args.build_dir`, matching the existing
`run_and_report(run_batch_build, idents, args.radius, args.provider, args.zl, args.build_dir)`
and `run_and_report(run_build, args.lat, args.lon, args.provider, args.zl, args.build_dir)`
call shapes.

Extend the `if __name__ == "__main__":` assert self-check block at the bottom of the file
(after the existing `_z = build_parser().parse_args(...)` zoom assertions) with new
assertions: parse `["build", "--icao", "KJFK", "--high-zl", "--cover-zl", "19"]` and assert
`.high_zl is True` and `.cover_zl == 19`; parse `["build", "47", "-122"]` and assert
`.high_zl is False` and `.cover_zl is None` (defaults).
  </action>
  <verify>
    <automated>cd "C:\Users\WillMcBurnett\dev\Ortho4XP" && venv\Scripts\python.exe src\O4_CLI_Utils.py</automated>
  </verify>
  <done>build_parser exposes --high-zl (store_true, default False) and --cover-zl (int, default None); run_build sets tile.cover_airports_with_highres = "ICAO" when high_zl is True and tile.cover_zl = cover_zl when cover_zl is not None; both params flow dispatch -> run_batch_build -> run_build; self-check block passes end to end with new assertions covering both flags and their defaults.</done>
</task>

</tasks>

<verification>
`venv\Scripts\python.exe src\O4_CLI_Utils.py` prints `O4_CLI_Utils self-check OK` with no
assertion errors. `O4_DSF_Utils.py` and `O4_Cfg_Vars.py` remain untouched (`git diff --stat`
shows only `src/O4_CLI_Utils.py`).
</verification>

<success_criteria>
`python Ortho4XP.py build 47 -122 --high-zl --cover-zl 19` (or the ICAO/batch equivalent)
constructs a `Tile` with `cover_airports_with_highres == "ICAO"` and `cover_zl == 19` before
the pipeline stages run, with zero changes outside `src/O4_CLI_Utils.py`.
</success_criteria>

<output>
Create `.planning/quick/260826-f6k-add-high-zl-and-cover-zl-flags-to-the-cl/260826-f6k-SUMMARY.md` when done
</output>
